#!/bin/bash -l

set -eu

# Run simulation
# $1: config file

if [ "$#" -le "0" ] ; then
  c=""
else
  c="$1"
fi

. ch.setenv

echo starting > job.status

c="nohup time mpirun -n 1 --report-pid pid ch.mfer '$c' &> a.log &"
echo "$c"
eval "$c"

echo running > job.status

