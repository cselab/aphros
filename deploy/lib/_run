#!/bin/bash

set -eu

P=$APHROS_PREFIX
. ../util

e () {
  echo "$@"
  eval "$@"
}

l="chmath csv table vtk vtkcolor split"
for d in $l
do (
    e cd $d
    echo Entering `pwd`
    e make install PRE=$PRE CC=cc
) done
