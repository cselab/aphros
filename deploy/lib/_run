#!/bin/bash

set -eu

. ch.setenv
. ../util

echo "EIGEN3_INCLUDE_DIR=${EIGEN3_INCLUDE_DIR}"

(cd vofi && ./conf && make install)
(cd overlap && ./conf && make install)
(cd vof && ./conf && make install)
(cd h5 && ./conf && make install)
