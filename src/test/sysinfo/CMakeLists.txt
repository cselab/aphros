get_filename_component(name ${CMAKE_CURRENT_SOURCE_DIR} NAME)
set(EXE t.${name})
add_executable(${EXE} main.cpp)
target_link_libraries(${EXE} sysinfo)
add_test_current(COMMAND ${EXE})
set_tests_properties(${name} PROPERTIES RUN_SERIAL true)
