function(add a0)
  set(name ${a0}${ARGN})
  add_test_current(NAME ${name} COMMAND run ${a0} ${ARGN})
endfunction()

add(periodic)
add(slipwall)
add(surfsymm)
add(surfwall)
add(wall)
add(inlet)
add(inletflux)
add(periodic proj)
add(slipwall proj)
add(surfsymm proj)
add(surfwall proj)
add(wall proj)
add(inlet proj)
add(inletflux proj)
