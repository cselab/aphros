function(add a0)
  set(name bc-${a0}${ARGN})
  add_test(NAME ${name} COMMAND run ${a0} ${ARGN})
  set_tests_properties(${name} PROPERTIES RESOURCE_LOCK bc)
endfunction()

add(periodic)
add(slipwall)
add(surfsymm)
add(surfwall)
add(wall)
add(inlet)
add(inletflux)
add(periodic proj)
