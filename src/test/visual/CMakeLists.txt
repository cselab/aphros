if (USE_DIM2)
  get_filename_component(name ${CMAKE_CURRENT_SOURCE_DIR} NAME)
  set(T t.${name})
  add_executable(${T} main.cpp)
  target_link_libraries(${T} mesh suspender logger vars parser filesystem format utilsystem)
  add_test_current(COMMAND ${T})
endif()
