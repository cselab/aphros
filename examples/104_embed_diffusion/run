#!/bin/bash

set -eu

for cs in 0 1 2 ; do
  d="case${cs}"
  echo $d
  ./main ${cs}
  mkdir -p $d
  [ "$i" = "0" ] && ./plot u_0000.dat && mv u_0000.svg $d
  ./plot u_0001.dat && mv u_0001.svg $d
done
