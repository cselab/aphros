awk '
BEGIN {
    OFS = ","
    n = 20
    r = 0.05
    print "x", "y", "z", "r", "vx", "vy", "vz"
    vx = vy = vz = 1
    for (i = 0; i < n; i++) {
        print rnd(), rnd(), rnd(), r, vx, vy, vz
    }
}

function rnd() {
    return 2*(rand() - 0.5)
}
'
