#!/bin/bash

a="partit_0000.csv"
o="a.csv"

# line number
ln=2
l=`sed "$ln!d" $a`
#c=`sed -r 's/\d*,\d*,(\d*).*/\1/' <<< "$l"`
#c=`cut -d',' -f4 <<< "$l"`
c=10026031
echo $c

echo "x,y,z,c" > $o
grep ",$c" $a >> $o
