#!/bin/bash -l

make -C ../../ install
ap-conf . -DEXTRA='-grid=multigrid3D -DCURV_PARTSTR'
make
ba.env ba.run main.s

a=0
r=ref
for f in $r/* ; do
  echo $f
  if ! ap-diffnum "${f#$r/}" "$f" "1e-14" sort rel; then
    a=1
  fi
done

exit $a
